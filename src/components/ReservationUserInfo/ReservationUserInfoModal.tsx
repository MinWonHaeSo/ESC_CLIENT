import React from 'react';

interface ReservationUserInfoModalProps {
  id: number;
}

const ReservationUserInfoModal = ({ id }: ReservationUserInfoModalProps) => {
  return <div>{id}</div>;
};

export default ReservationUserInfoModal;
